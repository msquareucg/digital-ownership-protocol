[project]
name = "digital-ownership-protocol"
authors = []
description = "A comprehensive Clarity smart contract system for physical asset tokenization and fractional ownership management"
telemetry = false
requirements = []

[contracts.asset-ledger]
path = "contracts/asset-ledger.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
